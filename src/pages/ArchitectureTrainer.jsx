import { LayoutDashboard } from 'lucide-react'
import { ComingSoon } from '../components/v2/ComingSoon'

export default function ArchitectureTrainer() {
  return <ComingSoon icon={LayoutDashboard} title="Architecture Trainer" subtitle="Break UIs into components and decide where state lives." />
}
